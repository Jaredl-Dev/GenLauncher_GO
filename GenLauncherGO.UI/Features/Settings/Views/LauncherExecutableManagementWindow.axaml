<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:launcherModels="using:GenLauncherGO.UI.Features.Launcher.Models"
        xmlns:localization="using:GenLauncherGO.UI.Shared.Localization"
        xmlns:vm="using:GenLauncherGO.UI.Features.Settings.ViewModels"
        x:Class="GenLauncherGO.UI.Features.Settings.Views.LauncherExecutableManagementWindow"
        x:ClassModifier="internal"
        x:Name="ManagementWindow"
        x:DataType="vm:LauncherExecutableManagementViewModel"
        x:CompileBindings="True"
        Title="{Binding WindowTitle}"
        Width="720"
        Height="610"
        CanResize="False"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        WindowDecorations="None"
        Background="{DynamicResource GenLauncherDarkBackGround}"
        Foreground="{DynamicResource GenLauncherDefaultTextColor}">
    <Window.Resources>
        <ControlTheme x:Key="ExecutableManagerListBoxItemTheme"
                      TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource GenLauncherDefaultTextColor}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="Template">
                <ControlTemplate>
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                </ControlTemplate>
            </Setter>
            <Style Selector="^:focus-visible">
                <Setter Property="BorderBrush" Value="{DynamicResource GenLauncherBorderColor}" />
            </Style>
        </ControlTheme>
    </Window.Resources>

    <Window.Styles>
        <Style Selector="Button.manager-action">
            <Setter Property="MinHeight" Value="33" />
            <Setter Property="Padding" Value="12,4" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <Style Selector="Button.row-edit">
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource GenLauncherDefaultTextColor}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style Selector="Button.row-edit:pointerover">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource GenLauncherDefaultTextColor}" />
        </Style>
        <Style Selector="Button.row-edit:pressed">
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style Selector="ListBoxItem:pointerover Border.executable-row">
            <Setter Property="Background" Value="{DynamicResource GenLauncherNeutralSurfaceColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource GenLauncherBorderColor}" />
        </Style>
        <Style Selector="ListBoxItem:selected Border.executable-row">
            <Setter Property="Background" Value="{DynamicResource GenLauncherSubtleAccentFillColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource GenLauncherNeutralBorderColor}" />
        </Style>
        <Style Selector="ListBoxItem:selected:pointerover Border.executable-row">
            <Setter Property="Background" Value="{DynamicResource GenLauncherSubtleAccentFillColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource GenLauncherBorderColor}" />
        </Style>
        <Style Selector="TextBox.manager-search">
            <Setter Property="MinHeight" Value="35" />
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Styles>

    <Border x:Name="ManagementFrame"
            Margin="14"
            Background="{DynamicResource GenLauncherNeutralSurfaceColor}"
            BorderBrush="{DynamicResource GenLauncherBorderColor}"
            BorderThickness="4">
        <Grid RowDefinitions="54,Auto,*,58">
            <Border x:Name="ManagementHeader"
                    Background="#FF000000"
                    BorderBrush="{DynamicResource GenLauncherBorderColor}"
                    BorderThickness="0,0,0,2">
                <TextBlock Margin="20,0"
                           VerticalAlignment="Center"
                           FontSize="20"
                           FontWeight="Bold"
                           Text="{Binding WindowTitle}" />
            </Border>

            <Grid Grid.Row="1"
                  Margin="18,16,18,8"
                  ColumnDefinitions="*,12,Auto">
                <TextBox x:Name="ExecutableSearchInput"
                         Grid.Column="0"
                         Classes="manager-search"
                         Text="{Binding SearchText, Mode=TwoWay}"
                         PlaceholderText="{localization:Loc SearchExecutables}" />
                <Button x:Name="AddExecutableButton"
                        Grid.Column="2"
                        Classes="manager-action"
                        MinWidth="142"
                        Content="{localization:Loc AddExecutable}"
                        Click="Add_ClickAsync" />
            </Grid>

            <ListBox x:Name="ExecutableEntries"
                     Grid.Row="2"
                     Margin="18,4,18,14"
                     Background="Transparent"
                     BorderBrush="{DynamicResource GenLauncherNeutralBorderColor}"
                     BorderThickness="1"
                     ItemContainerTheme="{StaticResource ExecutableManagerListBoxItemTheme}"
                     ItemsSource="{Binding Entries}"
                     SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="launcherModels:ExecutableOption">
                        <Border Classes="executable-row"
                                Margin="0,0,0,6"
                                Padding="11,8"
                                Background="{DynamicResource GenLauncherNeutralSurfaceColor}"
                                BorderBrush="{DynamicResource GenLauncherNeutralBorderColor}"
                                BorderThickness="1">
                            <Grid ColumnDefinitions="25,*,Auto,Auto">
                                <TextBlock Classes="missing-executable-indicator"
                                           VerticalAlignment="Center"
                                           FontSize="19"
                                           FontWeight="Bold"
                                           Foreground="#FFFF4E58"
                                           Text="&#x2715;"
                                           ToolTip.Tip="{localization:Loc ExecutableUnavailable}"
                                           IsVisible="{Binding IsUnavailable}" />
                                <StackPanel Grid.Column="1"
                                            IsVisible="{Binding IsBuiltIn}">
                                    <TextBlock FontSize="15"
                                               FontWeight="Bold"
                                               Text="{Binding DisplayName}" />
                                    <TextBlock Margin="0,3,0,0"
                                               FontSize="12"
                                               Foreground="{DynamicResource GenLauncherSecondaryTextColor}"
                                               Text="{Binding ExecutableName}" />
                                </StackPanel>
                                <Button Grid.Column="1"
                                        Classes="row-edit"
                                        Tag="{Binding}"
                                        ToolTip.Tip="{localization:Loc EditExecutable}"
                                        IsVisible="{Binding CanRemove}"
                                        Click="Edit_ClickAsync">
                                    <StackPanel>
                                        <TextBlock FontSize="15"
                                                   FontWeight="Bold"
                                                   Text="{Binding DisplayName}" />
                                        <TextBlock Margin="0,3,0,0"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource GenLauncherSecondaryTextColor}"
                                                   Text="{Binding ExecutableName}" />
                                    </StackPanel>
                                </Button>
                                <Border Grid.Column="2"
                                        Margin="10,0"
                                        Padding="7,3"
                                        VerticalAlignment="Center"
                                        BorderBrush="{DynamicResource GenLauncherNeutralBorderColor}"
                                        BorderThickness="1"
                                        IsVisible="{Binding IsBuiltIn}">
                                    <TextBlock FontSize="12"
                                               Text="{localization:Loc BuiltIn}" />
                                </Border>
                                <Button Grid.Column="3"
                                        Classes="manager-action"
                                        VerticalAlignment="Center"
                                        Content="{localization:Loc Remove}"
                                        IsVisible="{Binding CanRemove}"
                                        Tag="{Binding}"
                                        Click="Remove_Click" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="3"
                    Background="#FF000000"
                    BorderBrush="{DynamicResource GenLauncherBorderColor}"
                    BorderThickness="0,2,0,0">
                <Button Classes="manager-action"
                        Width="116"
                        HorizontalAlignment="Right"
                        Margin="0,12,18,12"
                        Command="{Binding CloseCommand}"
                        Content="{localization:Loc Close}" />
            </Border>
        </Grid>
    </Border>
</Window>
